# Copy to .env for local overrides (`make run` sources it; `make dev` supplies defaults itself).
ENV=local
PORT=8080
LOG_LEVEL=info
DATABASE_URL=postgres://keysmith:keysmith@localhost:5433/keysmith?sslmode=disable
AUTO_MIGRATE=false
